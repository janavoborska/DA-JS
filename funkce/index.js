/*
const elipsa = (height, width) => {
  return ((height * width) / 2) * Math.PI;
};

console.log(elipsa(4, 5));

const max2 = (number1, number2) => {
  if (number1 > number2) {
    return number1;
  } else {
    return number2;
  }
};

console.log(max2(4, 6));
*/

/*
const inputStr = prompt('Zadej dic');
const isDic = (inputStr) => {
  if (inputStr.length < 11) {
    return false;
  }

  if (inputStr.length > 12) {
    return false;
  } else {
    return true;
  }
};

console.log(isDic(inputStr));

const prefix = inputStr.slice(0, 2);
const cz = (prefix) => {
  if (prefix === 'CZ') {
    return true;
  } else {
    return false;
  }
};
console.log(cz(prefix));

const digitis = inputStr.slice(2, 12);
const jeCislo = (digitis) => {
  if (validator.isInt(digitis)) {
    return true;
  } else {
    return false;
  }
};
console.log(jeCislo(digitis));
*/
